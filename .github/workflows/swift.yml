name: Swift

on:
  workflow_call:
    inputs:
      swift-version:
        required: true
        type: string
      platform:
        required: true
        type: string

jobs:
  build:
    runs-on: ${{ inputs.platform }}
    name: Swift ${{ inputs.swift-version }}
    steps:
      - name: ☑️ Swift Select
        uses: swift-actions/setup-swift@v1
        with:
            swift-version: ${{ inputs.swift-version }}

      - name: ⬇️ Get Sources
        uses: actions/checkout@v3.5.3

      - name: 🍀 Test Package
        run: swift test
